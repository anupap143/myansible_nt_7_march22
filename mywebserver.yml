---
takes:
- name: i want to install ansible on ubuntu nodes
  apt:
   name: apache2
   state: present
 
 
- name: i want to start ansible on ubuntu nodes
  apt:
   name: apache2
   state: started
   

